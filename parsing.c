#include "philosophers.h"

void    parsing(t_push *main_prog, char *str)
{
    // int i = 0;
    main_prog->args  = ft_split(str, ' ');
    // while(main_prog->args[i])
    // {
    // if(ft_atoi(main_prog->args[i]) <= 0)
    //     ft_error();
    // }
}